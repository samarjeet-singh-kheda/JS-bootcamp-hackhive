//! this code will not throw an error bcoz of how js works (WHY???????)

const minusOne = (num) => {
  return num - 1;
};

const minusTwo = (num) => {
  return num - 1;
  dudu;
  bababababab;
};

console.log(minusOne(6));
